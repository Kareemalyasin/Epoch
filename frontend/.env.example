# Base URL of the FastAPI backend.
# Vite only exposes env vars to client-side code if they're prefixed with VITE_ —
# unprefixed vars (e.g. plain API_URL) are silently unavailable in the browser.
VITE_API_URL=http://127.0.0.1:8000
